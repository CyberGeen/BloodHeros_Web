import { Post } from './../common/Post';
import { debounce } from 'lodash';

export class UpDownVote extends Post {
    
    // const fun = debounce (array fun , intervale)

    //check in an array (main)

    //check up or down 

    //add id to array of up/down depend on whats provided



    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default UpDownVote
